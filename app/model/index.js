const userModel = require('./userModel');
const gameModel = require('./gameModel');
const characterModel = require('./characterModel');
const newsModel = require('./newsModel');
const inviteModel = require('./inviteModel');
const mapModel = require('./mapModel');
const skillModel = require('./skillModel');
const itemModel = require('./itemModel');
const characteristicModel = require('./characteristicModel');

module.exports = {
    userModel,
    gameModel,
    characterModel,
    newsModel,
    inviteModel,
    mapModel,
    skillModel,
    itemModel,
    characteristicModel
};